

def dailyTemperatures(temperatures: list) -> list:
    # need to have a stack with tuples of (max(previous values), index)
    max_stack = []
    current

    # first for loop to set max_stack
    for temp in temperatures:
